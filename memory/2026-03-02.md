
## Update (11:26 AM) — GitHub Identity Fully Operational

### cortana-hd GitHub Setup Complete
- **Username**: `cortana-hd`
- **SSH key**: `~/.ssh/id_ed25519_cortana`
- **SSH config**: `Host github-cortana` alias in `~/.ssh/config` (HostName github.com, IdentityFile id_ed25519_cortana)
- **Git remotes**: `cortana` remote added to both repos (`git@github-cortana:hd719/cortana.git` and `git@github-cortana:hd719/cortana-external.git`)
- **Git identity**: `user.name=cortana-hd`, `user.email=hameldesai3@gmail.com` set locally on both repos
- **PAT (classic)**: `cortana-cli` token with `repo` scope, expires Apr 1 2026. Used for GitHub API calls (PR creation, reviews).
- **Workflow**: Push via `git push cortana <branch>`, create PRs via `curl` with PAT. Do NOT use `gh` CLI for cortana-hd operations (gh is still authed as hd719).

### PR Flow Going Forward
- Cortana pushes branches + creates PRs as `cortana-hd`
- Hamel reviews and approves as `hd719`
- Branch protection: 1 required reviewer enforced on both repos
- PR #57 was the first PR created by cortana-hd

### Important Notes
- `origin` remote still uses hd719's SSH key — use `cortana` remote for all Cortana pushes
- `gh` CLI remains authenticated as hd719 — use for repo admin (rulesets, collaborators) but NOT for cortana-hd PR creation
- PAT expires Apr 1 2026 — needs renewal before then
