# Daily Upgrade – 2026-02-28

Git: committed pending changes via daily auto-commit.

Yesterday's gap: Heartbeat and health checks validated only cron execution, not delivery. Cron jobs (including the morning brief) were marked as "executed" even when lastDelivered=false / lastDeliveryStatus="not-delivered" for ~1.5 hours, so failures in Telegram delivery went undetected.

Proposed fix: Extend the heartbeat/healthcheck logic to treat delivery status as a first-class signal. Specifically:
- Parse cron state (jobs.json) and identify any jobs with lastDelivered=false or lastDeliveryStatus != "delivered" within the last N minutes.
- For those jobs, raise an immediate incident in cortana_events and surface an alert to Hamel (via a direct Telegram message) indicating which jobs failed delivery and since when.
- Update HEARTBEAT.md / heartbeat logic to consider "executed but not delivered" as FAILED, not OK, so the daily heartbeat will go red when delivery breaks.
- Add a small regression check in the watchdog/heartbeat script to ensure delivery checks remain enabled (e.g., a self-test that asserts at least one job has a recent lastDeliveryStatus field and logs if missing).

Effort: Medium (touches heartbeat scripts + cron state parsing + alerting wiring, but localized and testable).

Autonomy gain: When Telegram or cron delivery breaks, the system will detect and escalate it automatically instead of silently passing heartbeats. This prevents "green" dashboards during real outages, makes the morning brief and other cron outputs trustworthy, and reduces manual debugging when delivery fails.
